// Re-export the useCurrency hook from the currency provider
// This provides a consistent import path for components that expect the hook in the hooks directory

export { useCurrency } from "@/lib/currency-provider"

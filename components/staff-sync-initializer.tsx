'use client';

/**
 * DEPRECATED: Component to initialize staff data synchronization
 * Staff data is now fetched directly from the database API
 * This component is kept for compatibility but does nothing
 */
export function StaffSyncInitializer() {
  // This component doesn't render anything and is deprecated
  return null;
}
